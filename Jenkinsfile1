
pipeline {
    agent any
    tools{
        maven 'MAVEN_HOME'
    }
    stages {
        
        stage('Checkout') {
            steps {
             sh 'rm -rf port'
             sh 'git clone https://github.com/VINITHAS06/port.git'
            }
        }
        stage('deploy') {
            steps {
             sh 'configFileProvider([configFile('settings.xml_global')]) {
    // some block
}'
            }
        }
        }
        }
